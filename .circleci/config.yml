version: '2.1'
orbs:
  matlab: mathworks/matlab@0.11.0
jobs:
  build:
    machine:
      image: 'ubuntu-2204:2022.07.1'
    steps:
      - checkout
      - matlab/install
      - run:
          name: prep
          command:
            mkdir -p test-results
      - matlab/run-tests:
          test-results-junit: test-results/matlab/results.xml
      - store_test_results:
          path: test-results
      - matlab/run-command:
          command: /matlab/test/test.m
workflows:
  build:
    jobs:
      - build
